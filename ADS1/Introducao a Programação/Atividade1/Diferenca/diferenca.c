#include <stdio.h>

int main()
{
    int n1, n2, diferenca;
    
    scanf("%d", & n1);
    scanf("%d", & n2);
    
    diferenca = (n1 - n2);
    
    printf ("A diferença entre os dois números é de %d", diferenca);

}